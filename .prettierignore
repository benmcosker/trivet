.next/
node_modules/
src/generated/
prisma/migrations/
package-lock.json

# Design references, not source. The .dc.html is the design's source of truth
# and reformatting it would edit the artefact we are implementing against.
# Globbed rather than listed: this is the second such folder and the pass
# after this one will bring a third.
design_handoff_*/
